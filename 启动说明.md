# 校园创新创业项目管理平台 - 后端启动说明

## 一、项目目录结构

```
I&E Project/
│
├── main.py                              # FastAPI 应用入口（启动文件）
├── requirements.txt                     # Python 依赖清单
├── .env                                 # 环境变量配置
├── 启动说明.md                          # 本文档
│
├── app/
│   ├── __init__.py
│   │
│   ├── core/                            # 核心基础组件
│   │   ├── __init__.py
│   │   ├── config.py                    # pydantic-settings 全局配置
│   │   ├── response.py                  # 统一响应模型(ResponseModel/PageResult)
│   │   ├── exceptions.py                # 自定义异常 + 全局异常处理器注册
│   │   ├── security.py                  # JWT双Token、密码bcrypt、角色权限依赖、角色枚举
│   │   ├── deps.py                      # 分页/操作上下文/行级数据权限范围 依赖
│   │   └── middleware.py                # CORS + 操作日志审计中间件
│   │
│   ├── database/
│   │   ├── __init__.py
│   │   ├── session.py                   # SQLAlchemy 2.0 Engine/SessionLocal/get_db 依赖
│   │   └── schema.sql                   # MySQL 8.0 完整建表 SQL（13张表+初始化数据）
│   │
│   ├── models/                          # ORM 数据模型层
│   │   ├── __init__.py                  # 统一导出
│   │   ├── base.py                      # BaseModel基类(主键+时间戳+软删除)
│   │   ├── user.py                      # 用户/学院/日志/字典/附件
│   │   └── project.py                   # 项目/团队/审核/预算/报销/成果/中期/变更
│   │
│   ├── schemas/                         # Pydantic v2 请求/响应模型层
│   │   ├── __init__.py
│   │   ├── user.py                      # 登录/用户CRUD/学院/日志
│   │   ├── project.py                   # 项目申报/预算/团队/成果/统计
│   │   └── review.py                    # 审核/中期检查/变更申请
│   │
│   ├── crud/                            # 数据访问层(CRUD封装)
│   │   ├── __init__.py
│   │   ├── user.py                      # User/College/Log CRUD
│   │   ├── project.py                   # Project/Achievement CRUD + 编号生成 + 数据权限
│   │   └── review.py                    # Review/Midterm/Change CRUD
│   │
│   ├── services/                        # 业务逻辑层(Service)
│   │   ├── __init__.py
│   │   ├── user_service.py              # AuthService/UserService/CollegeService/LogService
│   │   ├── project_service.py           # ProjectService/AchievementService + 统计趋势
│   │   └── review_service.py            # ReviewService/MidtermService/ChangeService
│   │
│   ├── utils/                           # 工具类(技术特性落地)
│   │   ├── __init__.py
│   │   ├── redis_cache.py               # Redis单例 + @cached装饰器 + CacheKeys命名规范
│   │   ├── excel_helper.py              # ColumnDef校验 + 模板生成 + 导入解析 + 导出
│   │   └── search_engine.py             # Whoosh+Jieba 全文检索(索引/搜索/高亮/重建)
│   │
│   └── api/
│       ├── __init__.py
│       └── routers/                     # 路由层
│           ├── __init__.py
│           ├── user_router.py           # 认证 / 用户CRUD / 学院 / 操作日志
│           ├── project_router.py        # 项目申报CRUD / 成果 / 统计分析
│           ├── review_router.py         # 三级审核 / 中期检查 / 变更延期
│           └── common_router.py         # 附件上传下载 / Excel模板&导入导出 / 全文检索 / 字典缓存
│
├── static/
│   ├── uploads/                         # 附件上传目录
│   └── whoosh_index/                    # Whoosh全文检索索引目录
│
└── alembic/                             # (可选)数据库迁移目录
    └── versions/
```

## 二、技术栈回顾

| 层次 | 技术 | 版本 | 作用 |
|---|---|---|---|
| Web框架 | FastAPI | 0.115+ | 核心框架 / OpenAPI自动生成 / 异步支持 |
| ASGI服务器 | Uvicorn | 0.30+ | 运行FastAPI |
| 数据校验 | Pydantic v2 | 2.9+ | 请求/响应模型 + 校验规则 |
| ORM | SQLAlchemy 2.0 | 2.0.35+ | 声明式ORM + 连接池 |
| 数据库 | MySQL | 8.0+ | 主存储 (root密码 2023011630) |
| 认证 | python-jose + passlib | - | JWT双Token + bcrypt密码哈希 |
| 缓存 | Redis | 5.0+ | 字典/统计/公示名单缓存（自动降级）|
| Excel | openpyxl | 3.1+ | 模板生成 / 逐行校验导入 / 列表导出 |
| 全文检索 | Whoosh 2.7 + Jieba | - | 项目7字段中文索引+高亮+重建 |
| 配置 | pydantic-settings | 2.5+ | .env 类型安全加载 |

## 三、数据库核心表设计要点

核心 13 张表（详见 app/database/schema.sql）：

| 表名 | 用途 | 关键字段 |
|---|---|---|
| sys_user | 用户（4类角色统一）| role(1学生2教师3专家4管理员), college_id, status |
| sys_college | 学院 | college_code, dean_id |
| proj_project | 项目 | status状态机(0-11), leader_id, teacher_id, college_id, budget_amount |
| proj_team_member | 团队成员 | project_id + student_id 唯一 |
| proj_review | 审核记录 | review_stage(1-4阶段), review_result(1-3), score(专家评分) |
| proj_budget | 预算科目 | budget_item, budget_amount, used_amount |
| proj_expense | 报销申请 | 多级审批status(0-7) |
| proj_achievement | 成果 | 论文/专利/软著/竞赛/创业/其他 |
| proj_midterm_check | 中期检查 | project_id唯一 |
| proj_change_request | 变更/延期申请 | 多级审批status(0-4) |
| sys_attachment | 附件元数据 | biz_type + biz_id 关联任意业务 |
| sys_operation_log | 操作日志审计 | 中间件自动写入，模块/IP/耗时/参数 |
| sys_dict | 系统字典 | dict_type + dict_code 唯一 |

**项目状态机**：`0草稿 → 1待学院初审 → 2学院初审通过 → 3待校级复审 → 4校级通过 → 5待专家评审 → 6已立项 → 7中期 → 8待结题 → 9已结题`，异常：`10已驳回 / 11已撤销`。

## 四、环境准备（Windows）

### 4.1 安装 MySQL 8.0
1. 下载 MySQL 8.0，`root` 密码设为 `2023011630`（与.env一致）。
2. 打开 MySQL 客户端（Workbench 或 Navicat 或命令行），执行 SQL 脚本建表：
   ```sql
   source D:/SoftwareEngineering/I&E Project/app/database/schema.sql
   ```
   （若路径报错，可复制文件内容手动执行。）
3. 脚本会自动创建 `ie_project_db` 数据库 + 13张表 + 5条示例用户 + 6个学院 + 字典。
   若密码不同，请修改 `.env` 中 `DB_PASSWORD=你的密码`。

### 4.2 安装 Redis（推荐，非必需）
- Windows 推荐：下载 [Memurai](https://www.memurai.com/)（Redis兼容）或 WSL 安装 Redis。
- 启动后默认 127.0.0.1:6379，无密码即可。
- **未安装也可运行**：RedisClient 会自动降级为本地内存缓存（生产环境建议安装）。

### 4.3 Python 虚拟环境与依赖
```powershell
# 进入项目根目录
cd "D:\SoftwareEngineering\I&E Project"

# 1. 创建虚拟环境 (Python 3.10+)
python -m venv .venv

# 2. 激活虚拟环境
.venv\Scripts\activate

# 3. 安装依赖
pip install -r requirements.txt
```

## 五、启动项目

```powershell
# 虚拟环境激活后，任意一种方式即可：

# 方式1：直接运行 main.py（带 DEBUG reload）
python main.py

# 方式2：uvicorn 命令启动
uvicorn main:app --host 0.0.0.0 --port 8000 --reload

# 生产环境推荐多worker
uvicorn main:app --host 0.0.0.0 --port 8000 --workers 4
```

启动成功控制台显示：
```
============================================================
  校园创新创业项目管理平台 v1.0.0 启动中...
  文档地址:  http://0.0.0.0:8000/docs
============================================================
[OK] 数据库表结构已同步...
[OK] Redis 缓存连接成功
[OK] 全文检索索引已就绪
```

## 六、验证运行

1. 打开 Swagger 接口文档：<http://127.0.0.1:8000/docs>
2. 打开 ReDoc 文档：<http://127.0.0.1:8000/redoc>
3. 健康检查：<http://127.0.0.1:8000/api/health> 返回 200 OK。
4. **登录测试**：在 /docs 点击右上角 `Authorize`，或调用 `/api/auth/login`：

   | 账号 | 密码 | 角色 |
   |---|---|---|
   | admin | admin123 | 系统管理员 |
   | student001 | admin123 | 学生 |
   | teacher001 | admin123 | 指导教师 |
   | expert001 | admin123 | 评审专家 |

   登录成功会得到：`access_token`（短期）+ `refresh_token`（长期）+ 用户信息。
   后续请求加 Header：`Authorization: Bearer <access_token>`。

5. **全文检索初始化（推荐执行一次）**：
   登录 admin 后，POST `/api/search/rebuild-index` 即可把现有项目数据建索引。

## 七、业务流程演示（推荐顺序）

1. **学生端**：student001 登录 → `POST /api/projects` 创建草稿 → `POST /api/projects/{id}/submit` 提交审核。
2. **学院初审**：teacher001 登录 → `POST /api/reviews` review_stage=1 review_result=1（通过）。
3. **校级复审**：admin 登录 → `POST /api/reviews` review_stage=2 通过。
4. **分配专家**：admin → `POST /api/reviews/assign-experts` project_id=... expert_ids=[4]。
5. **专家评审**：expert001 登录 → `POST /api/reviews/expert` 打分 85 通过。
6. **状态查看**：此时项目状态=6（已立项），`GET /api/projects/{id}` 查看审核全流程。
7. **中期检查 → 成果登记 → 结题验收**：按上述状态机顺序操作即可。
8. **统计分析**：`GET /api/statistics/overview` 看总览；`GET /api/statistics/trend` 按月趋势（ECharts适配）。
9. **Excel导入用户**：下载模板 `/api/excel/template/user` → 填好 → `POST /api/excel/import/user`（admin）。
10. **全文检索**：`GET /api/search/projects?keyword=AI`，返回含 `<mark>` 高亮片段与 score。

## 八、生产部署要点

1. 把 `.env` 中 `DEBUG=False`，修改 `JWT_SECRET_KEY` 为强随机串。
2. 数据库密码改成非 root，创建专用数据库账号（仅 SELECT/INSERT/UPDATE/DELETE）。
3. 安装 Redis + 配置持久化（RDB/AOF）。
4. 使用 Nginx 反向代理 8000，配置 HTTPS、静态文件目录 `/static/uploads` 直接由 Nginx 提供。
5. 进程守护：推荐 `nssm`（Windows）注册系统服务，或 `pm2 start uvicorn -- main:app --port 8000`。
6. 日志：使用 `logging.basicConfig(level=INFO)` 或 loguru 接入。
7. 数据库迁移：生产使用 alembic（已预留 alembic/versions 目录，`alembic init alembic` 接入）。
8. 附件安全：建议将 `static/uploads` 放到对象存储（MinIO/阿里云 OSS），避免本地磁盘满。

## 九、常见问题

| 问题 | 解决方案 |
|---|---|
| MySQL 连接失败 | 检查 MySQL 服务是否启动；.env 中 DB_HOST/DB_PORT/DB_USER/DB_PASSWORD 是否正确；防火墙放行 3306。 |
| 中文乱码 | 建表脚本用的 utf8mb4，检查数据库连接串已加 ?charset=utf8mb4。 |
| Redis 报错但能正常启动 | 未装 Redis，已自动降级为本地内存缓存（重启丢失）。 |
| Whoosh 未安装 报 WARN | `pip install whoosh jieba` 后重启即可启用真正全文检索。 |
| admin 登录 401 | 检查 schema.sql 是否已执行（密码哈希 $2b$12... 需与 bcrypt 版本兼容）。 |
| Excel 导入失败行 | 返回的 ImportResultResponse.errors 字段给出具体 第X行:错误原因，修正后重传。 |
